# Dex autolaunches .desktop files that are set to do so
exec dex -ae i3

# Wallpaper
exec --no-startup-id systemctl --user start rotate-wallpaper.service

# Screen lock
exec --no-startup-id xss-lock -- i3lock -n -f -c 073642
