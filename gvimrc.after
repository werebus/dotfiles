" No Toolbar, No Scrollbars
set guioptions-=T
set guioptions-=r
set guioptions-=L
"
" Prety font
if has("gui_macvim")
  set guifont=Inconsolata:h14
else
  set guifont=Inconsolata\ Medium\ 20
endif

color jellybeans+

" A slightly bigger default window
set lines=35
set columns=90
